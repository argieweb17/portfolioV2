{
  "name": "repo2-workspace",
  "private": true,
  "workspaces": [
    "Portfolio"
  ],
  "scripts": {
    "dev": "npm run dev --workspace Portfolio",
    "build": "npm run build --workspace Portfolio",
    "start": "npm run start --workspace Portfolio",
    "lint": "npm run lint --workspace Portfolio"
  }
}